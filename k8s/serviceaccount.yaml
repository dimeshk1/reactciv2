apiVersion: v1
kind: ServiceAccount
metadata:
  name: github-actions-deployer
  namespace: github-actions
  labels:
    app: github-actions-deployer
  annotations:
    description: "ServiceAccount for GitHub Actions to deploy to EKS"
